<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/title_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Управление отношениями и контактами"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"/>

    <TextView
        android:id="@+id/description_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/title_text"
        android:text="Здесь вы можете связать семейные отношения с конкретными контактами в вашей адресной книге. Например, связать 'мама' с контактом 'Анна Иванова'."
        android:textSize="14sp"
        android:layout_marginBottom="16dp"/>

    <ListView
        android:id="@+id/relation_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/description_text"
        android:layout_above="@+id/add_relation_button"
        android:divider="#CCCCCC"
        android:dividerHeight="1dp"
        android:layout_marginBottom="16dp"/>

    <Button
        android:id="@+id/add_relation_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="Добавить новое отношение"
        android:textAllCaps="false"
        android:backgroundTint="@color/design_default_color_primary"/>

</RelativeLayout> 